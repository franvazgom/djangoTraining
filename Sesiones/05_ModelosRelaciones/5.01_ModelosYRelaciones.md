# Práctica Aplicación Blog
1.	Crear la aplicación “blog” 
2.	Crear los modelos  “Category”  y “Post” 
```python
from django.db import models
from django.utils import timezone
from django.contrib.auth.models import User

class Category(models.Model):
    name = models.CharField(max_length=100, verbose_name='Nombre')
    created = models.DateTimeField(
        auto_now_add=True, verbose_name='Fecha de creación')
    updated = models.DateTimeField(
        auto_now=True, verbose_name='Fecha de actualización')

    class Meta:
        verbose_name = 'Categoría'
        verbose_name_plural = 'Categorías'
        ordering = ['-name']

    def __str__(self):
        return self.name

class Post(models.Model):
    title = models.CharField(max_length=100, verbose_name='Título')
    content = models.TextField(verbose_name='Contenido')
    published = models.DateTimeField(
        default=timezone.now, verbose_name='Fecha de publicación')
    image = models.ImageField(
        upload_to='blog', null=True, blank=True, verbose_name='Imágen')
    author = models.ForeignKey(
        User, verbose_name='Autor', on_delete=models.CASCADE)
    categories = models.ManyToManyField(
        Category, verbose_name='Categorías', related_name='get_posts')
    created = models.DateTimeField(
        auto_now_add=True, verbose_name='Fecha de creación')
    updated = models.DateTimeField(
        auto_now=True, verbose_name='Fecha de actualización')

    class Meta:
        verbose_name = 'Post'
        verbose_name_plural = 'Posts'
        ordering = ['-created']

    def __str__(self):
        return self.title
```
3. Revisar documentación:
    + https://docs.djangoproject.com/en/6.0/search/?q=relationships
4.	Delegar la administración del CRUD al admin 
```python
from django.contrib import admin
from .models import Category, Post

class CategoryAdmin(admin.ModelAdmin):
    readonly_fields = ('created', 'updated',)

class PostAdmin(admin.ModelAdmin):
    readonly_fields = ('created', 'updated',)

admin.site.register(Category, CategoryAdmin)
admin.site.register(Post, PostAdmin)
```
5. Agregar la aplicación en el archivo settings.py
6. Verificar que se encuentre la aplicación django_cleanup
7. Realizar las migraciones correspondientes
8. Verificar su funcionamiento
    + Crear 3 usuarios al menos  Password: Zow#29Pv.
    + Colocar nombre y apellido al usuario Admin    
    + Agregar 2 o 3categorías
    + Agregar 2 o 3 Post’s 
9. Crear la estructura de directorios para el template
10. Crear el archivo post_list.html
11. Debes hacer un ciclo para recorrer todos los post
```python
<div class="container">
      {% for post in posts %}
        <div class="row">
```
12. El formato de fecha lo puedes resolver mediante: 
```python
<span class="section-heading-upper">{{post.published|date:"d/m/Y"}}</span>
```
Puedes revisar la documentación en: https://docs.djangoproject.com/en/6.0/ref/templates/builtins/#date

13. No olvides la imágen, debes de validar que exista la imágen 
```python
src="{{post.image.url}}
```
14. Las categorías, recuerda que pueden ser más de 1 
```python
<span class="section-heading-under">Publicado por <em><b>{{post.author.first_name}}</b></em> en <em>
    {% for category in post.categories.all %}
        <a href="#" class="link">{{category.name}}</a> {% if not forloop.last %},{% endif %}
    {% endfor %}
</em></span>
```
15. Crear la vista
```python
from django.shortcuts import render
from .models import Post, Category

def blog(request):
    posts = Post.objects.all()
    return render(request, 'blog/post_list.html', {'posts': posts})
```
16. Crear el archivo urls.py
```python
from django.urls import path
from blog import views

blog_urlpatterns = ([
    path('', views.blog, name='post'),
], 'blog')
```
16. Modificar el archivo urls.py del proyecto
17. Modificar el archivo base.html para incluir ésta opción
18. Verificar su funcionamiento

# Práctica - Complemento Blog
1. Crear la vista de category
```python
def category(request, category_id):
    category = Category.objects.get(id=category_id)
    return render(request, 'blog/category.html', {'category': category})
```
2. Agregar el url correspondiente
```python
blog_urlpatterns = ([
    path('', views.blog, name='post'),
    path('category/<int:category_id>', views.category, name='category'),
], 'blog')
```
3. Modificar el archivo post_list.html para mostrar las categorías y el autor
```python
{% for category in post.categories.all %}
    <a href="{% url 'blog:category' category.id %}" 
    class="link">{{category.name}}</a> {% if not forloop.last %},{% endif %}
{% endfor %}
```
4. La página category.html es una copia de todo el contenido de la página post_list.html, por lo que es conveniente crear la página blog.html (con todo que se encuentra dentro del for)
5. Modificar la página post_list.html para incluir la página blog.html
```python
<div class="container">
    {% for post in posts %}
    {% include "blog/blog.html" %}
    {% endfor %}
</div>
```
6. Crear la página category.html, pero ahora se realiza ingeniería en reversa para la consulta. Se puede personalizar el “related_name”, en el modelo, realizar el siguiente cambio. 
```python
categories = models.ManyToManyField(
        Category, verbose_name='Categorías', related_name='get_posts')
```
7.	Ahora se cambia el nombre en la página category.html
```python
... 
{% for post in category.get_posts.all %}
... 
```
8. Cambiar el menú de opciones, utilizar el filtro “slice”
```python
... {% if request.path|slice:':6' == '/blog/' %}active{% endif %} ... 
```
9. Verificar el funcionamiento

<b> La solución se encuentra en la carpeta v2.0 </b>