# Práctica agregar costo a los servicios
1. Modificar el modelo Service y realizar las migraciones correspondientes. 
```python
from django.core.validators import MinValueValidator, MaxValueValidator
    ...
    cost = models.DecimalField(
        default=0.0,
        max_digits=8, decimal_places=2, 
        validators=[MinValueValidator(0.0), MaxValueValidator(10000.0)]
    )
    ...
```
2. Modificar el formulario
```python
from django.forms import ModelForm, TextInput, Textarea, NumberInput
...
class ServiceForm(ModelForm):
    class Meta:
        model = Service
        fields = ['title', 'sub_title', 'content', 'image','cost',]
        widgets = {
            ... 
            'cost':NumberInput(attrs={'step': '0.01', 'placeholder': '0.00', 'class': 'form-control'}), 
        }
```
3. Modificar service_list.html
```html
...
{{service.content|safe}}
<br>
<p class="text-secondary text-end">
    $ {{service.cost}}
</p>
```
4. Verificar funcionamiento

# Práctica carrito de compras 
## Localstorage
+ Los datos persisten entre ventanas/tabs con el mismo origen.
+ Las claves y los valores son siempre cadenas de texto (ten en cuenta que, al igual que con los objetos, las claves de enteros se convertirán automáticamente en cadenas de texto).
+ Ejemplos
    + localStorage.setItem('miGato', 'Juan');
    + var cat = localStorage.getItem('miGato');
    + localStorage.removeItem('miGato');
    + localStorage.clear(); //Elimina Todos los elementos
## FrontEnd JavaScript 
1. En el archivo service_list.html
```html
<a class="btn btn-primary" onclick="addCart({{ service.id }});" >Agrega al carrito</a>
```
2. En el archivo base.html  
```html
<li class="nav-item px-lg-4">
    <a class="nav-link text-secondary" href="#" onclick="orderView();" >
        <i class="fas fa-shopping-cart"> <span id="cart-badge" class="badge badge-danger">0</span> </i>
    </a>
</li>
```
3. En el archivo base.html - JS 

Creación del carrito y despliegue 
```javaScript
    if (localStorage.getItem('cart') === null) {
      localStorage.clear(); 
      var cart = {};
    } else {
      cart = JSON.parse(localStorage.getItem('cart')); 
      showBadgeCart(); 
    }

    function showBadgeCart(){
      var totalItems = 0; 
      for (var item in cart) {
        totalItems += parseInt(cart[item]); 
      }
      const cartBadge = document.getElementById('cart-badge'); 
      cartBadge.innerText = totalItems.toString(); 
    }    
```
4. En el archivo base.html - JS 
```javaScript
    function addCart(id) {
      if (cart[id] != undefined) {
        cart[id] += 1; 
      } else {
        cart[id] = 1; 
      }
      localStorage.setItem('cart', JSON.stringify(cart)); 
      showBadgeCart(); 
    }
```
5. Verifca que se visualice el número correspondiente en el elemento cart-badge 

## Formulario y envío de información 
1. En el archivo base.html
```html
...
</footer>

  <form id="data_form" action="{% url 'services:order_request' %}" method="post" >
    {% csrf_token %}
    <input type="hidden" name="data_order" id="data_order" />
  </form>
  
... 
```
2. En el archivo base.html - JS 
```javaScript
function orderView(){    
      cart_str = localStorage.getItem('cart');       
      const dataOrder = document.getElementById('data_order');
      const dataForm = document.getElementById('data_form');
      dataOrder.value = cart_str;
      dataForm.submit(); 
    }
```
## BackEnd construcción del detalle del pedido 
1. Vista order_request 
```python
from django.views.decorators.http import require_POST
import json
...
@require_POST
def order_request(request):
    order = list()
    total = 0
    try:
        data_order = json.loads(request.POST['data_order'])
        for key, qty in data_order.items():
            detail = {}
            service = Service.objects.get(pk=int(key))
            detail['title'] = service.title
            detail['cost'] = float(service.cost)
            detail['qty'] = qty            
            detail['sub_total'] = float(service.cost) * qty
            total += detail['sub_total']
            order.append(detail)
    except json.JSONDecodeError as e:
        print(f"Error parsing. Details: {e}")                
    # Se guarda el total y el detalle del pedido en la sesión
    request.session['total_order'] = total
    request.session['detail_order'] = order
    return render(request, 'services/detail_order.html', {'order':order, 'total':total})
```
2. Agregar el url correspondiente
```python
... 
path('order_request', views.order_request, name='order_request')
```
3. Crear archivo detail_order.html
```html
{% extends "core/base.html" %}
{% block content %}
{% load static %}  
    <div class="container">
      <div class="row mt-2">
        <div class="col-md-9 mx-auto">
            <table class="table table-dark table-striped table-bordered">
                <thead class="text-white text-center">
                    <tr>
                        <th>Producto</th>
                        <th>Cantidad</th>
                        <th>Precio</th>
                        <th>Subtotal</th>
                    </tr>
                </thead>
                <tbody class="text-white">
                    {% for product in order %}
                      <tr>
                        <td>{{product.title}}</td>
                        <td style='text-align:right;' >{{product.qty}}</td>
                        <td style='text-align:right;' >{{product.cost}}</td>
                        <td style='text-align:right;'>$ {{product.sub_total}}</td>                        
                      </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
      </div>
      <div class="row mt-2">
        <div class="col-md-7"></div>
        <div class="col-md-2 mx-auto mb-2">
          <div style='text.align:right'>
            <span class="text-white">Total = $ {{total}}</span>
          </div>
        </div>
      </div>
      <div class="row mt-2">
        <div class="col-md-9 mx-auto mb-1">
          <div id='button_submit' style='text-align:right;'>
            <a href="XXXX" class="btn text-white form-btn mx-1">              
              Confirmar pedido<i class="fas fa-paper-plane fa-fw"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
{% endblock  %}
{% block contentJS %}
  <script>

  </script>
{% endblock contentJS %}
```
4. Verifica su funcionamiento 

## Guardado del pedido
1. Crear el modelo Order
```python
class Order(models.Model):
    name = models.CharField(max_length=100, verbose_name='Nombre')
    address = models.CharField(max_length=200, verbose_name="Dirección")
    total = models.DecimalField(verbose_name='Total', max_digits=8, decimal_places=2)
    email = models.EmailField(verbose_name='Email')
    date = models.DateTimeField(auto_now_add=True, verbose_name='Fecha')

    class Meta:
        verbose_name = 'Pedido'
        verbose_name_plural = 'Pedidos'
        ordering = ['-date']        

    def __str__(self):
        return str(self.id)
```
2. Realizar las migraciones correspondientes
3. Delegar el CRUD al admin
```python
class OrderAdmin(admin.ModelAdmin):
    readonly_fields = ('date', )
    ordering = ('name', )
    search_fields = ('name', 'email', )
    list_display = ('name', 'total', 'date', )

admin.site.register(Order, OrderAdmin)
```
4. Crear el formulario
```python
class OrderForm(ModelForm): 
    class Meta: 
        model = Order
        fields = ['name', 'address', 'total', 'email']
        widgets = {
            'name':TextInput(attrs={'class':'form-control', 'placeholder':'Nombre'}),
            'address':TextInput(attrs={'class':'form-control', 'placeholder':'Dirección'}),
            'email':EmailInput(attrs={'class':'form-control', 'placeholder':'Email'}),
            'total':TextInput(attrs={'class':'form-control', 'readonly':'readonly'}),
        }
```
5. Crear las vistas order_create y order_thanks
```python
def order_create(request):
    # Get total
    total = request.session.get('total_order')
    if request.method == 'POST':
        form = OrderForm(request.POST)
        if form.is_valid():
            form.save()
            return HttpResponseRedirect(reverse_lazy('services:thanks'))            
    else:  # Método get
        form = OrderForm(initial={'total':total})
    return render(request, 'services/order_create.html', {'form': form})

def order_thanks(request):
    return render(request, 'services/order_thanks.html')
```
6. Crear los correspondientes urls
```python
path('order_create', views.order_create, name='order_create'),
path('order_thanks', views.order_thanks, name='thanks'),
```
7. En detail_order, actualizar el url 
```html
<a href="{% url 'services:order_create' %}" class="btn text-white form-btn mx-1">
```
8. Crear  order_create.html
```html
{% extends "core/base.html" %}
{% block content %}
{% load static %}  
    <div class="container">
        <form action="" id='formOrder' novalidate class="px-0 py-2 pb-5" method='post'>
            {% csrf_token %}
            <table class="text-white">
                {{form.as_table}}
            </table>        
            
            <div class="row mt-2">                       
                <div class="col-md-9 mx-auto mb-1">
                    <div id='button_submit' class='float-right'>
                        <button type='submit' class="btn text-info form-btn mx-1" style='width:170px;' >
                            <span class="spinner-border spinner-border-sm d-done"></span>
                            <i class="fas fa-paper-plane fa-fw"></i>Continuar
                        </button>                                                            
                    </div>
                </div>
            </div>
        </form>
    </div>
{% endblock  %}
```
9. Crear order_thanks.html
```html
{% extends "core/base.html" %}
{% block content %}
{% load static %}  
    <div class="container">              
        <div class="row mt-2">                       
            <div class="col-md-9 mx-auto mb-1">
                <h1 class="text-white">Gracias por su compra</h1>                
            </div>
        </div>    
    </div>
{% endblock  %}
{% block contentJS %}
  <script>
    cart = {};
    localStorage.clear();
    showBadgeCart();
  </script>
{% endblock contentJS %}
```
10. Verificar su funcionamiento, y también datos en el ADMIN del pedido

<b> Solución en code/soluciones/restaurante/v11.0 </b>