# CRUD Personalizado  (Services)
## Create
1.	Crear el archivo forms.py (ModelForm) 
```python
from django.forms import ModelForm
from services.models import Service

class ServiceForm(ModelForm):
    class Meta:
        model = Service
        fields = ['title', 'sub_title', 'content', 'image',]
```
2.	En el archivo views.py
```python
from django.shortcuts import render
from django.http import HttpResponseRedirect
from django.urls import reverse_lazy
from services.models import Service
from .forms import ServiceForm

def service_list(request):
    services = Service.objects.all()
    return render(request, 'services/service_list.html', {'services': services})

def create(request):
    if request.method == 'POST':
        form = ServiceForm(request.POST, request.FILES)
        if form.is_valid():
            form.save()
            return HttpResponseRedirect('/services')
    else:
        form = ServiceForm()
    return render(request, 'services/service_create.html', {'form':form})
```
3.	En el archivo urls.py
```python
from django.urls import path
from services import views

services_urlpatterns = ([
    path('', views.service_list, name='services'),
    path('create/', views.create, name='create'),
], 'services')
```
4.	Crear el archivo service_create.html
```html
{% extends 'core/base.html' %}
{% block content %}
{% load static %}
{% include 'services/menu_services.html' %}
<section class="page-section about-heading">
    <div class="container">      
        <div class="row mt-3">
          <div class="col-md-9 mx-auto text-light ">
            <div>                            
              <form action='' method='post' novalidate enctype='multipart/form-data'>
                {% csrf_token %}
                <table>
                    {{ form.as_table }}
                </table>
                <div class="text-center">
                  <input type="submit" class="btn btn-primary btn-block py-2" value="Crear servicio">
                </div>
              </form>              
            </div>
          </div>
        </div>      
    </div>
  </section>
{% endblock %}
```
5.	Crear el archivo menu_services.html
```html
{% if request.user.is_staff %}
    <nav class="navbar navbar-expand-lg navbar-dark py-lg-4" id="SecondNav">
        <div class="container">
            <span class="navbar-brand"> <i>Servicios</i></span>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarServices"
                aria-controls="navbarServices" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarServices">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{% url 'services:create' %}"> + Nuevo servicio</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endif %}
```
Agregar el estilo en business-casual.css 

```css
#SecondNav {
  background-color: rgba(47, 23, 15, 0.5);
}
```
6.	En el archivo service_list.html
```python
{% extends 'core/base.html' %}
{% block content %}
{% include 'services/menu_services.html' %}
{% for service in services %}
```
7.	Verificar su funcionamiento. 

## Práctica
1.	Modificar el modelo 
```python
content = CKEditor5Field('Contenido', config_name='extends', blank=True, null=True)
```
2.	Modificar el formulario para agregar widgets y para agregar validaciones. 
```python
from django.forms import ModelForm, TextInput, Textarea
from services.models import Service
from django_ckeditor_5.widgets import CKEditor5Widget

class ServiceForm(ModelForm):
    class Meta:
        model = Service
        fields = ['title', 'sub_title', 'content', 'image',]
        widgets = {
            'title':TextInput(attrs={'class':'form-control','placeholder':'Título'}),
            'sub_title':TextInput(attrs={'class':'form-control','placeholder':'Sub título'}),
            'content':CKEditor5Widget(config_name='extends'),
        }
    
    def clean(self):
    ...
```
3.	Modificar el html que contiene el formulario
```html
<form action='' method='post' novalidate enctype='multipart/form-data'>
    {% csrf_token %}
    <table>
        {{ form.as_table }}
    </table>
    <br>
    {{ form.media }}
    <div class="text-center">
        <input type="submit" class="btn btn-primary btn-block py-2" value="Crear servicio">
    </div>
</form>              
```
4. Realizar las migraciones correspondientes
5. Verificar su funcionamiento

## Update View
1.	Crear la el método update
```python
def update(request, service_id):
    service = Service.objects.get(id=service_id)
    if request.method == 'POST':
        form = ServiceForm(request.POST, request.FILES, instance=service)
        if form.is_valid():
            form.save()
            return HttpResponseRedirect('/services')
    else:
        form = ServiceForm(instance=service)
    return render(request, 'services/service_update.html', {'form':form})
```
2.	Modificar el archivo urls.py
```python
path('update/<int:service_id>', views.update, name='update'),
```
3.	Ya que el código es muy similar al código del archivo service_create.html, crear el archivo  “service_form.html” con el contenido del formulario, de la siguiente manera. 
```html
<section class="page-section about-heading">
    <div class="container">      
        <div class="row mt-3">
          <div class="col-md-9 mx-auto text-light ">
            <div>                            
              <form action='' method='post' novalidate enctype='multipart/form-data'>
                {% csrf_token %}
                <table>
                    {{ form.as_table }}
                </table>
                <br>
                {{ form.media }}
                <div class="text-center">
                  <input type="submit" class="btn btn-primary btn-block py-2" value="{{ btn_action }}">
                </div>
              </form>              
            </div>
          </div>
        </div>      
    </div>
</section>
```
4.	Modificar el archivo “service_create.html”
```python
{% extends 'core/base.html' %}
{% load static %}
{% include 'services/menu_services.html' %}
{% block content %}
{% include 'services/service_form.html' with btn_action="'Crear servicio'" %}
{% endblock %}
```
5.	Crear el archivo service_update.html igual que service_create.html
```python
...
{% include 'services/service_form.html' with btn_action="'Actualizar servicio'" %}
...
```
6.	Modificar el archivo service_list.html
```html
<div class="bg-faded p-5 rounded">                
    {{service.content|safe}}
    <br>
    {% if request.user.is_staff %}
        <a href="{% url 'services:update' service.id %}">Ver detalle</a>
    {% endif %}
</div>
```
7.	Verificar su funcionamiento

## Seguridad
Verifique que pasa si accede de manera directa a la página de creación o de actualización sin estar logeado como administrador

```python
from django.contrib.admin.views.decorators import staff_member_required
... 
@staff_member_required()
def create(request):
    ... 
```
## Delete View
Realizar de manera similar al update la parte correspondiente al Delete. 

<b> Solución en code/soluciones/restaurante/v9.0 </b>